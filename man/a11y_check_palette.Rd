% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a11y_check_palette.R
\name{a11y_check_palette}
\alias{a11y_check_palette}
\title{Check a palette against WCAG contrast thresholds}
\usage{
a11y_check_palette(colors, bg = "#ffffff", level = "AA", alpha = 1)
}
\arguments{
\item{colors}{Character vector of hex codes.}

\item{bg}{Reference background hex(es). Pass a single value (e.g.
\code{"#ffffff"}) or a vector (e.g. \code{c("#ffffff", "#1a1a1a")}) to verify
against multiple backgrounds. Returns one row per (color, bg) pair.}

\item{level}{\code{"AA"} (4.5:1) or \code{"AAA"} (7:1). Use \code{"AA-large"} (3:1) for
non-text elements such as data marks, axis lines, and focus rings.}

\item{alpha}{Opacity in \verb{[0, 1]}. \code{1} (default) checks the raw color.
Values below 1 composite each color over \code{bg} first, then check the
rendered result -- useful when chart geoms use \code{alpha < 1}.}
}
\value{
Data frame with columns \code{color}, \code{bg}, \code{alpha}, \code{rendered},
\code{ratio}, \code{status}.
}
\description{
Computes the contrast ratio of every color against a reference background
and reports whether each meets the WCAG threshold (4.5:1 for AA, 7:1 for
AAA). When \code{alpha < 1}, contrast is computed against the alpha-composited
rendered color -- the color the viewer actually sees.
Works on any palette -- \code{viridis}, \code{RColorBrewer}, plotly's discrete
sequences, or a custom hex vector.
}
\examples{
a11y_check_palette(c("#000000", "#cccccc", "#ff0000"))
a11y_check_palette(c("#0072B2"), bg = c("#ffffff", "#1a1a1a"), level = "AA-large")
a11y_check_palette(c("#0072B2", "#D55E00"), alpha = 0.7, level = "AA-large")
}
