% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a11y_describe.R
\name{a11y_describe}
\alias{a11y_describe}
\title{Generate alt text via a user-supplied LLM backend}
\usage{
a11y_describe(p, backend, attach = TRUE)
}
\arguments{
\item{p}{A \code{ggplot} or \code{plotly} object.}

\item{backend}{A function \verb{function(context) -> character(1)}. Receives a
list with fields \code{chart_type}, \code{title}, \code{x}, \code{y}, \code{color},
\code{n_observations}. Returns a single string.}

\item{attach}{If \code{TRUE} (default), attach via \code{a11y_alt_text()}.
Otherwise return the string.}
}
\value{
The plot with alt text attached, or the string itself.
}
\description{
Extracts deterministic plot context (chart type, axes, ranges, group
counts) and passes it to the user's \code{backend} function, which calls any
LLM provider and returns the alt-text string. Result is attached to the
plot via \code{\link[=a11y_alt_text]{a11y_alt_text()}} when \code{attach = TRUE}.
}
\details{
The package itself depends on no LLM SDK; the caller supplies the
transport. See examples for OpenAI, Gemini, and Ollama backends.
}
\examples{
\dontrun{
  openai_backend <- function(context) {
    prompt <- paste(
      "Write one-sentence WCAG 1.1.1 alt text.",
      "State chart type, axes, and key trend."
    )
    auth <- paste("Bearer", Sys.getenv("OPENAI_API_KEY"))
    res <- httr::POST(
      "https://api.openai.com/v1/chat/completions",
      httr::add_headers(Authorization = auth),
      body = list(
        model = "gpt-4o-mini",
        messages = list(
          list(role = "system", content = prompt),
          list(role = "user",
               content = jsonlite::toJSON(context, auto_unbox = TRUE))
        )
      ),
      encode = "json"
    )
    httr::content(res)$choices[[1]]$message$content
  }
  p |> a11y_describe(backend = openai_backend)
}
}
