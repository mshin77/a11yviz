% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a11y_alt_template.R
\name{a11y_alt_template}
\alias{a11y_alt_template}
\title{Generate a deterministic alt-text template for a plot}
\usage{
a11y_alt_template(p)
}
\arguments{
\item{p}{A \code{ggplot} or \code{plotly} object.}
}
\value{
Character scalar.
}
\description{
Introspects a \code{ggplot} or \code{plotly} object and emits a sentence scaffold
with chart type, axis labels, ranges, and group counts pre-filled. A
bracketed placeholder marks where the substantive trend description
belongs. Pass the edited string to \code{\link[=a11y_alt_text]{a11y_alt_text()}} to attach it.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  library(ggplot2)
  p <- ggplot(mtcars, aes(wt, mpg, color = factor(cyl))) + geom_point()
  a11y_alt_template(p)
}
}
